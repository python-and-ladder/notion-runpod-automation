[project]
name = "simple-app-in-pyside6"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyside6>=6.10.2",
    "runpod",
]

[dependency-groups]
dev = ["pyinstaller"]
